"use strict";

/**
 * jsonXmlify
 * A JSON to XML converter.
 *
 * @name jsonXmlify
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function jsonXmlify (a, b) {
    return a + b;
};